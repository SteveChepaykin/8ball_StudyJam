import 'package:flutter/material.dart';

class BlackCircleWidget extends StatelessWidget {
  const BlackCircleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      alignment: Alignment.center,
      children: [
        Container()
      ],
    );
  }
}